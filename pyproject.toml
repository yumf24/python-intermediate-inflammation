[project]
name = "inflammation"
version = "0.1.0"
description = "This is a poetry test"
authors = [{ name = "yumf", email = "yumf828@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["matplotlib (>=3.11.1,<4.0.0)", "numpy (>=2.5.1,<3.0.0)"]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = ["pylint (>=4.0.6,<5.0.0)", "pytest (>=9.1.1,<10.0.0)"]
